//use polars::prelude::*;

// Number of threads for multiprocessing
/*static NUM_THREADS: i64 = 8;

// Wafel path and version
static DLL_PATH: &str = "D:\\wafel_0_8_5_win_x64";
static VERSION: &str = "us";

// Info of target m64
static ZERO_INDEX_FRAMES: bool = false;
static START_FRAME: i64 = 0; // Frame rng will be set on
static END_FRAME: i64 = 1;
static MOVIE_LENGTH: i64 = END_FRAME - START_FRAME;
static INP_NAME: &str = ".m64";

// Basis m64 info (for state loaded conversion)
// will do later
static STATE_LOADED: bool = false;
static OFFSET: i64 = 0;
static BASIS_NAME: &str = ".m64";

const OBJID_MEMORY_ADDR: i64 = 0;
const OBJID_WAFEL_SLOT: i64 = 1;
const OBJID_STROOP_SLOT: i64 = 2;

// Slot, start and end frames for coin info to be collected

pub fn main() {
    println!("{NUM_THREADS}");
    println!("{MOVIE_LENGTH}");
    println!("{DLL_PATH}");
}
*/
